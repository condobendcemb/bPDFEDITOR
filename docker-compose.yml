version: '3.8'
services:
  react-app:
    build: .
    ports:
      - "5008:3000"  # เปลี่ยนจาก 5173:5173 เป็น 5008:5173
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true